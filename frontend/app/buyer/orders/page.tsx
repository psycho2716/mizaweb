export default function BuyerOrdersPage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-semibold">Buyer Orders</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Order UI scaffold is ready. Connect this page to `/orders` backend
        endpoints as order state transitions are expanded.
      </p>
    </main>
  );
}
